define([
    'reports/elements/static/headers/headers-directives'

], function () {
    return angular.module('app.reports.elements.headers', [
        'app.reports.elements.headers.directives'
    ]);
});